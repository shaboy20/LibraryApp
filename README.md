# A book trading app

YOUR COMMIT NAMES MUST BEGIN WITH `patch:`, `feature:`, or `release:`

- If it's a small bugfix, use `patch:`.
- If it's a new feature, use `feature:`.
- If it's a polished, final release, use `release:`.
